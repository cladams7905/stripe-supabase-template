# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git (it's in .gitignore)

# ============================================
# SUPABASE SETTINGS
# ============================================
# Where to find these:
# 1. Go to https://supabase.com
# 2. Create a new project or select existing one
# 3. Go to Settings > API
# 4. Copy the values below

# Your Supabase project URL (looks like: https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here

# Your Supabase anonymous/public key (safe to use in browser)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# STRIPE SETTINGS
# ============================================
# Where to find these:
# 1. Go to https://stripe.com
# 2. Create account or login
# 3. Go to Developers > API keys
# 4. Copy the values below

# Your Stripe Publishable Key (safe to use in browser)
# For testing, use test keys (they start with pk_test_)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Your Stripe Secret Key (NEVER share or expose in browser!)
# For testing, use test keys (they start with sk_test_)
STRIPE_SECRET_KEY=your_stripe_secret_key_here

# Optional: Stripe Webhook Secret (for handling payment events)
# Get this when you set up webhooks in Stripe Dashboard
STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret_here

# ============================================
# APP SETTINGS
# ============================================
# Your app's URL (used for redirects and webhooks)
NEXT_PUBLIC_APP_URL=http://localhost:3000
